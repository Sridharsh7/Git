Here are some common git commands categorized based on their functions

Setting up a repository:

* `git init`: Creates a new Git repository.
* `git clone`: Creates a copy of an existing remote repository.

Working with changes:

* `git add`: Adds changed files to the staging area.
* `git commit`: Creates a snapshot of the changes in the staging area.
* `git status`: Shows the status of your working directory and staging area.

Branching and merging:

* `git branch`: Lists, creates, or deletes branches.
* `git checkout`: Switches between branches.
* `git merge`: Combines changes from different branches.

Remote repositories:

* `git remote`: Adds, removes, or lists remote repositories.
* `git pull`: Fetches changes from a remote repository and merges them into your local branch.
* `git push`: Pushes your local commits to a remote repository.
